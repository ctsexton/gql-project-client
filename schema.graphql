# source: http://localhost:4000
# timestamp: Mon May 27 2019 17:20:01 GMT-0700 (Pacific Daylight Time)

directive @cacheControl(maxAge: Int, scope: CacheControlScope) on FIELD_DEFINITION | OBJECT | INTERFACE

type Artist {
  id: ID!
  name: String!
  songs: [Song]!
}

enum CacheControlScope {
  PUBLIC
  PRIVATE
}

type Collaborator {
  id: ID!
  artist: Artist!
  share: Float!
}

type Query {
  artists: [Artist]!
  songs: [Song]!
  song(id: String): Song!
}

type Song {
  id: ID!
  name: String!
  artist: Artist!
  collaborators: [Collaborator]!
}

"""The `Upload` scalar type represents a file upload."""
scalar Upload
