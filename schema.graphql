# source: http://localhost:4000
# timestamp: Tue May 28 2019 00:06:32 GMT-0700 (Pacific Daylight Time)

directive @cacheControl(maxAge: Int, scope: CacheControlScope) on FIELD_DEFINITION | OBJECT | INTERFACE

input AddArtistInput {
  name: String!
}

type AddArtistPayload {
  id: ID!
  name: String!
}

type Artist {
  id: ID!
  name: String!
  songs: [Song]!
}

enum CacheControlScope {
  PUBLIC
  PRIVATE
}

type Collaborator {
  id: ID!
  artist: Artist!
  share: Float!
}

type Mutation {
  addArtist(input: AddArtistInput!): AddArtistPayload
}

type Query {
  artists: [Artist]!
  songs: [Song]!
  song(id: String): Song!
}

type Song {
  id: ID!
  name: String!
  artist: Artist!
  collaborators: [Collaborator]!
}

"""The `Upload` scalar type represents a file upload."""
scalar Upload
